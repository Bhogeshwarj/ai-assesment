# Frontend

Next.js (App Router) + TypeScript + Tailwind + shadcn/ui client for the Tea Packaging
Optimization Platform.

See the [repository root README](../README.md) for setup instructions, architecture,
and API documentation. Quick start:

```bash
npm install
cp .env.local.example .env.local
npm run dev
```
